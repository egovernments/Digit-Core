parent=keycloak
import=common/keycloak

# Load Keycloak's base login.css first, then our overrides on top
styles=css/login.css css/digit.css

meta=viewport==width=device-width,initial-scale=1

# Body class our template.ftl / CSS hook onto
kcBodyClass=kc-digit
