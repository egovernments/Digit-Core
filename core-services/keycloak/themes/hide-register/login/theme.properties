# hide-register: login theme that hides self-registration entirely.
#
# Inherits all DIGIT branding from digit-logo. Assign this theme per client
# (Admin console -> Client -> Settings -> Login settings -> Login theme)
# to disable self-registration for that client only, while realm
# registration stays ON for all other clients.
parent=digit-logo
import=common/keycloak
