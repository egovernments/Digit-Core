spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notify
    username: notify
    password: notify
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    baseline-on-migrate: true

notify:
  plugins:
    directory: ../providers

logging:
  level:
    org.digit.notify: DEBUG
