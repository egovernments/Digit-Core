import { createTheme } from '@mui/material/styles';

export const InjiTheme = createTheme({
    palette: {
        primary: {
            main: '#FF8F00',
            light: '#FFB600'
        },
        secondary: {
            main: '#FF8F00',
            light: '#FFB600'
        }
    },
});
